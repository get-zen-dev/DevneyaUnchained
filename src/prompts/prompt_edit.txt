I have multiple errors in code. Here is a code snipped:
``` javascript
    const pivot = array[0];
    cont left = [];
    const right = [;

    for (let i = 1; i < array.length; i++) {
      if (array[i] < pivot) {
        left.push(array[i]);
      } else {
        right.push(array[i]);
      }
    }

    return [
      ...Quicksort.sort(left),
      pivot,
      ...Quicksort.sort(right)
    ];   
  }

}
```